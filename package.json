{
  "name": "chat_app",
  "version": "0.0.1",
  "license": "ISC",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "concurrently 'firebase emulators:start --project demo-dev --export-on-exit=./emulators_data --import=./emulators_data' 'dart run build_runner watch -d'"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
